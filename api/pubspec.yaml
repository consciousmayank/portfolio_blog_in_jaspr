name: portfolio_api
description: Dart Shelf API backing the mayankjoshi.in portfolio + blog.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_multipart: ^2.0.0
  mime: ^2.0.0
  postgres: ^3.5.0
  dart_jsonwebtoken: ^3.2.0
  dbcrypt: ^2.0.0
  mailer: ^6.4.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
  yaml: ^3.1.2
